const tests = require('./../../../gest-print.js');
const returnedObj = tests();
const gets = returnedObj.gets;
const print = returnedObj.print;

const generateBinaryHeap = () => {
    const n = gets();
    do {
        const currElement = +gets();
        

        n -= 1;
    } while (n >= 0);
};
generateBinaryHeap();
